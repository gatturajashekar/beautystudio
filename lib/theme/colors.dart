import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFEBD8C3);   // Main background
  static const card = Color(0xFFF3DCC4);         // Card background
  static const light = Color(0xFFFFF3E5);        // Boxes, sheets
  static const accent = Color(0xFFE7B872);       // Appbar, highlights
  static const textDark = Color(0xFF4A3426);     // Text color
}
